"use client";

/** Potential Client marketing layout passthrough (role switches are intentional). */
export function PotentialClientExperienceGuard({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
